[deploy]
startCommand = "python manage.py migrate api --noinput && python manage.py migrate --noinput && gunicorn academia.wsgi --bind 0.0.0.0:$PORT"
releaseCommand = "python manage.py migrate api --noinput && python manage.py migrate --noinput"
